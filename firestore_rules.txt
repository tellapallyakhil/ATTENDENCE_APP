rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ WARNING: These rules allow anyone to read and write to your database.
    // This is useful for development and prototyping but is INSECURE for production.
    // Since you requested no authentication for now, we are using open rules.
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
